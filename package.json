{
  "name": "stat-ai-2.0-deployable",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir public && cp index.html public/ && cp -r aulas public/ && esbuild index.tsx --bundle --outfile=public/index.js --jsx=automatic"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
